# Checksums

Run `python scripts/data/verify_download.py --raw-dir data/raw --write-checksums` after downloading the source release. The generated SHA-256 file is intentionally ignored by default because source-version policy may differ; retain it with the project evidence when appropriate.
